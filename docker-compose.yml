services:
  db:
    image: postgres:16-alpine
    container_name: personal-me-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: personal_me
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app -d personal_me"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

  backend:
    build:
      context: ./backend
    container_name: personal-me-backend
    restart: unless-stopped
    env_file:
      - .env
    environment:
      POSTGRES_DSN: postgresql+psycopg://app:app@db:5432/personal_me
      JWT_SECRET_KEY: super-secret-change-me
      ACCESS_TOKEN_EXPIRE_MINUTES: 120
      REFRESH_TOKEN_EXPIRE_DAYS: 7
      PASSWORD_RESET_EXPIRE_MINUTES: 30
      INITIAL_ADMIN_USERNAME: admin
      INITIAL_ADMIN_PASSWORD: admin123
      ALLOW_REGISTRATION: "true"
      COOKIE_SECURE: "false"
      OIDC_ISSUER: http://localhost/api/v1/oidc
      OIDC_FRONTEND_BASE_URL: http://localhost
      OIDC_AUTO_APPROVE: "true"
      CORS_ORIGINS: http://localhost,http://localhost:3000,http://localhost:3010
      INTEGRATIONS_JSON: '{"grafana":{"url":"http://localhost:3001","requires_auth":true,"use_sso":true,"label":"Grafana"},"portainer":{"url":"http://localhost:9000","requires_auth":true,"label":"Portainer"},"billiards":{"url":"http://localhost/billiards/","requires_auth":true,"use_sso":true,"label":"Billiards Kolkhoz"}}'
      INITIAL_ADMIN_EMAIL: admin@localhost
      SMTP_HOST: mailpit
      SMTP_PORT: "1025"
      SMTP_FROM: noreply@localhost
      SMTP_USE_TLS: "false"
      EXPOSE_RESET_TOKEN: "false"
      UPLOADS_DIR: /app/uploads
    depends_on:
      db:
        condition: service_healthy
      mailpit:
        condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/')\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    ports:
      - "8000:8000"
    volumes:
      - uploads_data:/app/uploads

  frontend:
    build:
      context: ./frontend
    container_name: personal-me-frontend
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
      - frontend_nuxt:/app/.nuxt
    dns:
      - 8.8.8.8
      - 1.1.1.1
    environment:
      NUXT_PUBLIC_API_BASE_URL: ""
      NUXT_API_INTERNAL_URL: http://backend:8000
      NUXT_VITE_HMR_CLIENT_PORT: "80"
    depends_on:
      backend:
        condition: service_healthy
    ports:
      - "3000:3000"
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:3000/').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"
        ]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 180s

  billiards:
    build:
      context: ./apps/billiards
    container_name: personal-me-billiards
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./apps/billiards:/app
      - billiards_node_modules:/app/node_modules
      - billiards_nuxt:/app/.nuxt
    dns:
      - 8.8.8.8
      - 1.1.1.1
    environment:
      NUXT_PUBLIC_HUB_URL: http://localhost
      NUXT_PUBLIC_BRAND_NAME: DAUTOVTECH
      NUXT_PUBLIC_API_BASE_URL: ""
      NUXT_VITE_HMR_CLIENT_PORT: "80"
      HOST: 0.0.0.0
      PORT: "3010"
      NITRO_HOST: 0.0.0.0
      NITRO_PORT: "3010"
    ports:
      - "3010:3010"
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:3010/billiards/').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"
        ]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 180s

  nginx:
    image: nginx:1.27-alpine
    container_name: personal-me-nginx
    restart: unless-stopped
    depends_on:
      - frontend
      - backend
      - billiards
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./nginx/starting.html:/usr/share/nginx/html/starting.html:ro

  mailpit:
    image: axllent/mailpit:latest
    container_name: personal-me-mailpit
    restart: unless-stopped
    ports:
      - "8025:8025"
      - "1025:1025"

volumes:
  postgres_data:
  uploads_data:
  frontend_node_modules:
  frontend_nuxt:
  billiards_node_modules:
  billiards_nuxt:
